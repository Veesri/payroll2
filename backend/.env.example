# =============================================================
# PayrollPro — Backend Environment Example
# =============================================================
# Copy this file to .env and fill in your values.
# NEVER commit .env to git — only commit .env.example
# =============================================================

# -----------------------------------------------------------
# Django Core
# -----------------------------------------------------------
# Generate: python -c "import secrets; print(secrets.token_hex(64))"
SECRET_KEY=your-very-long-random-secret-key-here

DEBUG=True

# Comma-separated, no spaces
ALLOWED_HOSTS=localhost,127.0.0.1

# -----------------------------------------------------------
# JWT Secret
# -----------------------------------------------------------
JWT_SECRET_KEY=your-jwt-secret-key-here

# -----------------------------------------------------------
# Database
# -----------------------------------------------------------
# Leave blank for SQLite (dev), or set a full URL for production
DATABASE_URL=

# -----------------------------------------------------------
# CORS — comma-separated frontend origins (no trailing slash)
# -----------------------------------------------------------
CORS_ALLOWED_ORIGINS=http://localhost:5173

# -----------------------------------------------------------
# Email (SMTP)
# -----------------------------------------------------------
EMAIL_HOST=smtp-relay.brevo.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=<BREVO_SMTP_LOGIN>
EMAIL_HOST_PASSWORD=<BREVO_SMTP_KEY>
DEFAULT_FROM_EMAIL=PayrollPro <<VERIFIED_BREVO_SENDER>>


# -----------------------------------------------------------
# Frontend URL
# -----------------------------------------------------------
FRONTEND_URL=http://localhost:5173
